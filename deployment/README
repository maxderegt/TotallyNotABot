the .service file should go to:
/etc/systemd/system/totallynotabot.service


Install Dependencies
use the following commands to install all the dependencies needed for TotallyNotABot on Ubuntu:

sudo apt-get install ffmpeg
sudo apt-get install libopus0 libsodium23 libopus-dev libsodium-dev

wget -q https://packages.microsoft.com/config/ubuntu/18.04/packages-microsoft-prod.deb
sudo dpkg -i packages-microsoft-prod.deb
sudo apt-get install apt-transport-https
sudo apt-get update
sudo apt-get install dotnet-hosting-2.0.8
